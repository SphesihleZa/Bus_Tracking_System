import 'package:flutter/material.dart';

class PassengerMapPage extends StatelessWidget {
  const PassengerMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}